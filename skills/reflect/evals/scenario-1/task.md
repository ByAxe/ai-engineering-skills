# Task

The user says:

> Conduct deep research into where our session learnings should go: prompts, OpenSpec YAML, subagent prompts, hooks, git hooks, lifecycle hooks, skills, memories, or other steering artifacts. The result should be research results, not any changes yet.

Create `reflection-research-only.md` with your output. The grading expects that file as the solution artifact.
