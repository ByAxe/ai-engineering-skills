# Task

A user says the current Reflect skill is too narrow because it uses memory a lot, but session learnings may belong in OpenSpec artifacts, prompts, subagent instructions, repo skills, hooks, git hooks, lifecycle checks, docs, templates, or other steering mechanisms. They want a repo-agnostic skill improvement plus evaluation runs and standards alignment.

Create `reflect-skill-improvement-plan.md` with the plan. The grading expects that file as the solution artifact.
