# Task

A completed OpenSpec bugfix fixed a production identity-provider webhook delivery failure. It has a delta spec, proposal, design, tasks, regression tests, and all tasks are checked off. The user asks whether to run only `openspec-sync-specs` so the main specs know about the new webhook behavior.

Create `completed-bugfix-history-plan.md` explaining the desired completion workflow. Include what belongs in the main capability spec, what belongs in archived change history, when sync-only is acceptable, and which validations should happen before the bugfix is called complete. The grading expects that file as the solution artifact.
