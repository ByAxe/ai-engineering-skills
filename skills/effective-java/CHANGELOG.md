# Changelog

## 1.0.0 — 2026-07-10

- Replaces `java-refactoring` and `java-21-refactor-assessor` with one coherent skill.
- Adds evidence-first implementation, fix, assessment, modernization, optimization, and refactoring modes.
- Adds focused Java 21 language, API, collections, equality, time, I/O, concurrency, build, testing, security, persistence, performance, and compatibility references.
- Adds Quarkus build-time, CDI, REST, execution-model, persistence, configuration, security, observability, messaging, testing, Dev Services, and native-image guidance.
- Adds agent failure-mode taxonomy and anti-overengineering controls.
- Adds project profiler, heuristic scanner, diff-scope checker, wrapper-first quality gate, and bundle validator.
- Keeps optional quality-gate arguments compatible with Bash 3.2 when `set -u` is enabled.
- Adds Agent Skills and Tessl eval suites plus executable script tests.
- Adds a reproducible SHA-256 payload manifest and validation record with explicit external-tool limitations.
