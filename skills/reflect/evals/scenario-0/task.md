# Task

A team just finished a session where the agent repeatedly missed required verification commands, treated an independent reviewer as optional until the user corrected it, and then learned that a course-content smoke command can leave generated assets dirty.

The project has `AGENTS.md`, `.codex/hooks.json`, `.githooks/pre-push`, `openspec/changes/*`, `.agents/skills/*`, `docs/`, and local memory files.

Create `reflection-routing-report.md` with a reflection recommendation. The grading expects that file as the solution artifact.
