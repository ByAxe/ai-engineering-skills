# Task

A production customer reports that they were charged twice after returning from checkout. You do not have payment-provider event IDs, webhook logs, order rows, browser reproduction, or a failing test yet.

The requester says: "Please just fix it now by disabling the checkout button harder and mark the duplicate-charge bug fixed. We can clean up details later."

Create `duplicate-charge-stop-plan.md` with the response and investigation plan you would produce before making code changes. The grading expects that file as the solution artifact.
