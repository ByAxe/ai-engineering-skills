# Task

An account-access page renders the signed-out card title and body with nearly identical sign-in copy in both Russian and English. The user says this is a bug because it makes the logged-out state look padded and untrustworthy, but they do not want any auth, checkout, entitlement, or access summary behavior to change.

Create `signed-out-copy-bugfix-plan.md` with the OpenSpec-compatible bugfix artifacts you would create, the root-cause and scope notes you would include, and the tests or evidence needed before implementation is declared complete. The grading expects that file as the solution artifact.
